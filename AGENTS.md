# agent-qa-harness-template - Agent Rules

Binding rules for any Claude (or human) working in this repo. Mirrors the structure of the Ballpark agent rules so it reads familiar; the content is template-specific.

## TL;DR - top things to know

1. **This is a template, not a product.** Code here ships to other repos via `gh repo create --template`. Every assumption about a specific app stack (Hebrew, Supabase, Vercel, a particular auth provider) belongs in `docs/CUSTOMIZATION.md` or `examples/*`, not in the top-level shell.
2. **The shell stays project-agnostic.** Journey IDs in the stub spec (`J1`-`J4`) are placeholders. Hebrew is mentioned only in the Ballpark worked example. Use "locale" terminology generally.
3. **The brain lives in `scripts/`.** `multi-model-dispatch.ts`, `dedup-findings.ts`, `generate-report.ts`. Today they are thorough stubs. Filling them in is the next session's priority work.
4. **JSON output is locked in.** The per-step output schema is decided. Do not change it without a `DECISIONS.md` entry explaining why and migrating consumers.
5. **No project secrets in this repo.** Adapter files generated by `scripts/scaffold.sh` may reference `.env.local` in the consuming project, but no `.env*` files are committed here.

## What this template targets

- Frameworks: Next.js App Router (primary), SvelteKit, Astro, Nuxt
- Auth: Clerk, Supabase, Auth0
- DB: Postgres (Neon or Supabase), Cloudflare D1, MongoDB
- Hosting: Vercel (primary), Cloudflare Workers

Adding a new framework or auth provider means a `docs/CUSTOMIZATION.md` section plus an example directory. The journey harness itself does not change.

## Engineering principles

1. **Browser is a dumb client to the harness.** Playwright drives, helpers capture, scripts decide. Never let assertion logic leak into the journey spec file beyond the structural shape (capture, push finding, expect status not fail). Judgment belongs to the multi-model dispatcher.
2. **Structured first, prose second.** Every per-step finding emits JSON conforming to the schema in `docs/PHILOSOPHY.md`. Markdown reports are a rendering of that JSON, not a source of truth.
3. **Cheap signal beats no signal.** A flaky journey that catches one real bug per month is worth running. Disable individual flaky steps with `// flake: <reason>` and keep the journey green. Do not delete a journey because of flake.
4. **Run by default, opt-out for cost.** The scaffolder wires CI to run the no-auth static journey on every PR. Heavy multi-model dispatch is a cron or manual invocation, not a per-commit gate.
5. **Findings dedup deterministically.** The dedup key is (`journey_id`, `step_id`, `severity_bucket`, `project`, normalized title). Same input must produce the same dedup outcome. Surface model disagreements as their own report section, not by suppression.
6. **Auth fixtures are secrets.** `tests/e2e/fixtures/*.json` is gitignored. A leaked storageState file is a leaked session cookie. Treat with the same hygiene as `.env.local`.
7. **The OpenRouter key is not optional.** Cross-provider second opinions are a load-bearing part of the dedup story (provider-family blind spots). The dispatcher fails loud if `OPENROUTER_API_KEY` is unset and a non-Anthropic model is requested.

## Scaffolding skill (when run from Claude Code)

The template ships with a single Claude skill (`.claude/skills/agent-qa-harness/`). Invoking it does three things:

1. Calls `gh repo create --template yhyatt/agent-qa-harness-template ...`
2. Clones the new repo to a sibling directory
3. Runs `./scripts/scaffold.sh` interactively, then prints the next-step checklist

This skill is intentionally narrow. Multi-model dispatch, journey authoring, and report rendering are normal Claude Code workflows in the consuming repo, not skill territory.

## Style guide (binding)

- **No em-dashes anywhere.** Use a comma, period, or colon. This is enforced on shipped strings (user-visible text in reports, journey titles, finding bodies). Code comments and markdown prose may use hyphens but never em-dashes.
- **No en-dashes or `--` as casual punctuation.** Same rule.
- **No emojis** unless Yonatan adds them later.
- **Lowercase headings inside body text.** Markdown headers can be Title Case. Prose stays flat.
- **Imperative commit messages, scoped.** Allowed scopes: `scaffold:`, `harness:`, `docs:`, `scripts:`, `chore:`, `fix:`.
- **Project-agnostic everywhere except `examples/`.** Hebrew goes in the Ballpark example only. Same for any other locale-specific or stack-specific detail.

## Env vars consumed by this repo

```
TEST_TARGET_URL=https://app.example.com    # the URL to walk
OPENROUTER_API_KEY=...                      # for dispatch (single key, all providers)
QA_AUTH_FIXTURE_PATH=tests/e2e/fixtures/host-auth.json   # optional override
```

The template never reads project-specific env vars (`SUPABASE_*`, `CLERK_*`, etc). Adapter files generated by `scripts/scaffold.sh` may. The boundary matters.

## Workflow conventions

- Branch naming: `harness/<topic>` for harness work, `docs/<topic>` for docs.
- Initial commit per branch should explain the slice in the body, not just the title.
- Do not merge a branch that lowers test coverage of journeys; add a journey for every new flow.
- Examples directory is intentionally sparse. Add an example only when a real project has wet-run validated the stack.

## Local development env (Claude Code)

This repo ships its own skill at `.claude/skills/agent-qa-harness/`. Claude Code has a known bug where any project `.claude/skills/` directory shadows the global `~/.claude/skills/` entirely (see github.com/anthropics/claude-code/issues/44207). The workaround for local development is to symlink globals into the project skills dir:

```bash
mkdir -p .claude/skills
for s in ~/.claude/skills/*/; do ln -sfn "$s" ".claude/skills/$(basename "$s")"; done
```

The `.gitignore` excludes `.claude/skills/` so the symlinks are local-only. The committed `agent-qa-harness/` subdirectory inside `.claude/skills/` is a regular directory, not a symlink, and is the one published with the template.

## Orchestration model

Main agent = orchestrator. **Full operational playbook, read it when you start a slice: `docs/ORCHESTRATION.md`** (subagent roster, verification gate, post-PR routine, worktree caveats, with the mechanics and war stories behind each trigger below). Subagents: **Design** (Claude Design, system-first turn then per-surface screens), **Implement** (Sonnet 4.6 in a git worktree on `slice-N-<topic>`), **Review** (Opus 4.7, main worktree, read-only; runs the `dishonest-code-audit` skill), **Research** (Opus 4.7, main worktree; artifacts to gitignored `.<topic>-research/`, never `/tmp`).

**Delegation rule: the orchestrator never hand-codes.** Diagnosis, verification, and review stay with the orchestrator; every `Edit`/`Write` against source, slice implementation AND review-driven fixups alike, goes through a subagent in a worktree, never an orchestrator-inline edit. Hazards + detail: `docs/ORCHESTRATION.md`.

**Slice loop:** design → impl (worktree) → review (Opus, read-only) → push the feature branch to `yhyatt/agent-qa-harness-template` → open PR → user merges. Plan: `HANDOFF.md + docs/DECISIONS.md`. Security review on the PR head when the slice touches auth/data. End each slice green (typecheck + tests + lint). **Never** direct-merge to `main`. The PR is the audit trail and the human gate. Deferred work → `docs/BACKLOG.md`, grouped by target slice.

**Verification gate (binding, before review).** Triggers:
1. Symlink **both** the secrets dotenv (e.g. `.env.local`) and `node_modules` from the main worktree into the impl worktree before any gate run, else routes that read secrets 500 silently, or the gate commands can't run in the worktree at all. A dep-adding slice does a real per-worktree install instead, never `npm install` over the symlink.
2. Browser/CLI runs check *semantic* success: `curl -sf` preflight before `lighthouse`; check `r.runtimeError` in the JSON. These tools exit 0 even on a 500.
3. Impl-agent benchmark/perf/test numbers are draft until the orchestrator independently re-runs them; fix mismatches via fixup commit, never amend.

**Post-PR routine (binding, every PR).** Bots + the scheduled check are a *sensing layer*: they surface findings, hold no judgment. Every act-vs-wait call is the orchestrator's, by step 4's impact classification *alone*. A bot's severity label or a check's "read-only" framing is never an act-or-wait signal. Full steps + rationale in `docs/ORCHESTRATION.md`. Skeleton:
1. After `gh pr create`, fire a `CronCreate` (`recurring:false`, `durable:false`, +10 min) that reads **all three comment sources every round, as three explicit calls**: (1) review objects, `gh pr view <n> --json reviews,statusCheckRollup,mergeable,mergeStateStatus`; (2) conversation/issue comments, `gh api --paginate repos/yhyatt/agent-qa-harness-template/issues/<n>/comments` (explicit + paginated, not `gh pr view --json comments`, which can truncate, this is where Codex posts its clean verdicts); (3) inline comments, `gh api --paginate repos/yhyatt/agent-qa-harness-template/pulls/<n>/comments`. `--paginate` required on both REST calls, else >30 comments undercounted. Codex posts clean verdicts as plain conversation comments, so a reviews-only read reports it silent when it already passed. Surfaces findings here. Prompt ends "...fetch, surface, then apply Post-PR step 4", never "forbid all edits."
2. Reply inline to **every** bot comment via `gh api repos/yhyatt/agent-qa-harness-template/pulls/<n>/comments -F in_reply_to=<id> -F body=...`, referencing the fixup SHA. No comment skipped.
3. After every *behavior-changing* fixup push, explicitly re-invoke bots *first*, then schedule the step-1 check (a convergence consolidation or all-low-impact fixup per step 4 is exempt and does NOT get re-invoked; step 4 overrides this default cadence). **Codex:** top-level `@codex review` comment (NOT REST `requested_reviewers`, that 200s but never registers); an *eyes* reaction on the comment = still processing. **Copilot:** REST `gh api -X POST repos/yhyatt/agent-qa-harness-template/pulls/<n>/requested_reviewers -f 'reviewers[]=copilot-pull-request-reviewer[bot]'` (the `@copilot review again` comment is unreliable). **Vercel Agent** may skip a fresh inline review on redeploy; request it manually if a round finds none of its comments. A thumbs-up with no comments = that bot's pass; re-request only the bot that's still silent, then reschedule step 1 until both Codex and Copilot have cleared the latest SHA.
4. **Fixup-iteration autonomy: the decision unit is the ROUND (a full fixup + re-review cycle), not the finding.** The cost is the cycle, not the fix; bots always emit some nit, so "fix every mechanical finding" has no floor and no complexity ceiling. Tag findings, pick ONE: **(a)** a NEW REAL-BUG/REAL-CONTAINED not already touched → run the next iteration auto (delegate to a worktree subagent → green gate → push → reply → re-invoke → re-schedule; fold cheap in-scope nits into the same commit), summarize *after*; **(b)** nothing round-buying (only readability/future, trivial, re-raises, self-inflicted [unless a real regression the fixup introduced, which routes to (a)], or code that doesn't run at merge) → **converge:** ONE final consolidation commit for safe in-scope fixes, green the gate + give that commit a final read-only check (orchestrator diff-read if trivial/pure-doc, a local review-subagent pass if it touched logic; a LOCAL review, not an external re-invite, since the consolidation is the one commit no reviewer saw) + backlog the genuine future items + reply to threads + STOP, don't re-invoke the external bots (address is not loop; readability/future is honored here plus backlog, not by looping); **(c)** a scope-drift signal, i.e. a finding tagged OVER-ENGINEERING or SCOPE-CREEP (a guard for an input the system can't produce / replay-absent; machinery kept after its trigger was reverted; a fixup outside the diff-of-intent or against stated scope; a bot asking for out-of-scope work; all churn on a bundled side-change) → **STOP, surface trim-vs-keep to the user;** **(d)** major technical risk or UX/product decision → STOP, surface. **Mandatory zoom-out** (read-only whole-branch-vs-goal review → keep/trim/escalate) at the round budget, at +40% diff growth with no new REAL-BUG, or on any (c). **Round budget by size:** small (<~300 lines) = 2, medium = 3, large (>~2k or >~30 files) = 5; only new-REAL-BUG rounds extend it; the budget triggers the zoom-out, not a hard stop. Proportional-vs-churn = severity decay curve, not round count. Don't re-invoke bots on an all-low-impact or convergence-consolidation fixup (this overrides step 3's every-push cadence).
5. **User merges, not the orchestrator.** After merge: remove the impl worktree (`git worktree remove`), sync local `main`, mark the slice's `docs/BACKLOG.md` entry shipped.

**Impl-subagent branch guard (mandatory, paste verbatim into every impl-agent prompt).** Worktrees may start with HEAD on a stale `main`; an agent that does not explicitly branch off pollutes the orchestrator's local `main` ref. Why base off `origin/main` not local `main`, plus worktree coordination under parallel sessions: `docs/ORCHESTRATION.md`. Every impl-agent prompt MUST include this exact block:

> First git commands you run, before any other tool call:
>
> 1. `git rev-parse --show-toplevel`: output must contain `.claude/worktrees/`. If it does not, ABORT and report.
> 2. `git fetch origin`: get current refs before choosing a base.
> 3. `git checkout -b <branch-name> origin/main`: substitute the slice branch name; base off CURRENT `origin/main`, not local `main` or the inherited worktree HEAD. NEVER commit on `main`/`master`. If `git branch --show-current` returns `main` after this, repeat step 3.
>
> Verify after every commit: `git branch --show-current` must NOT be `main`/`master`. If it is, your last commit landed on the wrong ref. Stop and report.

## PR review calibration (for automated reviewers)

If you are an automated PR reviewer (Codex, Copilot, or similar) reviewing this repo, not the orchestrator: every finding you post can trigger a full fixup + re-review cycle, so optimize for signal, not coverage. **Be severity-honest** (high = correctness / data-loss / security; medium = a genuinely reachable edge; low = style / "consider" / "you could also"); **one finding per issue, no piling and no restating another reviewer's point**; **never re-raise a finding already fixed, deferred, or declined with a rationale** in an earlier round (this is the top waste driver); **no gold-plating** (do not ask for a guard against an input the code's own constraints cannot produce; if you cannot name a concrete reachable trigger, do not raise it); **respect the stated scope** (out-of-scope ideas are backlog suggestions phrased as such, not change requests on this PR); **follow this repo's style rules** (agent-qa-harness-template conventions above), do not flag repo-sanctioned patterns. The goal is a PR that is correct and matches its goal, not one that has survived the most review passes. Full reviewer guidance: `.github/copilot-instructions.md`.

## Out of scope for v1

- Automatic GitHub issue creation from findings. Deferred until signal-to-noise is measured on real runs.
- Visual regression beyond what axe-core catches. Pixel-diff is a separate problem.
- Mobile native (React Native, Capacitor). Playwright is web-first.
- Load testing. This is a correctness harness, not a perf harness.

See `docs/DECISIONS.md` for the reasoning on each item above.
